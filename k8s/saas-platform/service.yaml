apiVersion: v1
kind: Service
metadata:
  name: saas-platform
  namespace: artis
spec:
  type: NodePort
  selector:
    app: saas-platform
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081